export const checkQuestionOne = (answer: string): boolean => {
	if (answer === "Tokyo") {
		return true;
	} else {
		return false;
	}
};

export const checkQuestionTwo = (answer: string): boolean => {
	if (answer === "Foreshortening") {
		return true;
	} else {
		return false;
	}
};

export const checkQuestionThree = (answer: string): boolean => {
	if (answer === ".map()") {
		return true;
	} else {
		return false;
	}
};

export const checkQuestionFour = (answer: string): boolean => {
	if (answer === "Sunshine Airport") {
		return true;
	} else {
		return false;
	}
};

export const checkQuestionFive = (answer: string): boolean => {
	if (answer === "Series 5") {
		return true;
	} else {
		return false;
	}
};
